window.Cricket26Shaders={vertex:`varying vec3 vNormal; varying vec3 vWorld; void main(){vNormal=normalMatrix*normal;vec4 w=modelMatrix*vec4(position,1.0);vWorld=w.xyz;gl_Position=projectionMatrix*viewMatrix*w;}`,fragment:`varying vec3 vNormal; varying vec3 vWorld; uniform vec3 sunColor; void main(){float light=max(dot(normalize(vNormal),normalize(vec3(.4,1.,.3))),0.0);float haze=clamp(1.0-length(vWorld.xz)/90.0,0.0,1.0);vec3 c=mix(vec3(.03,.08,.05),sunColor,light*.72);c+=haze*.025;gl_FragColor=vec4(c,1.0);}`};